# Normalize line endings to LF in the repository; let Git handle
# checkout conversions per platform.
* text=auto eol=lf

# Binary files — never touch line endings
*.pptx binary
*.docx binary
*.xlsx binary
*.pdf  binary
*.png  binary
*.jpg  binary
*.svg  binary
